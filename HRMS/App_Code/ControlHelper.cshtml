@using System.Web.Mvc.Html;

@helper RenderTextBoxFormControl(System.Web.Mvc.HtmlHelper Html, string name, string value)
{
    @Html.TextBox(name, string.IsNullOrEmpty(value) ? string.Empty : value, //Value of the variable
            new { @class = "form-control", @type = "text" })
    @Html.ValidationMessage(name)
}

@helper RenderTextBoxFormControl(System.Web.Mvc.HtmlHelper Html, string name, DateTime value)
{
    @Html.TextBox(name, //Name of the variable
        value == DateTime.MinValue ? string.Empty : value.ToString("yyyy-MM-dd"), //Value of the variable
        new { @class = "form-control", @type="date" })
    @Html.ValidationMessage(name)
}

@helper RenderEmailBoxFormControl(System.Web.Mvc.HtmlHelper Html, string name, string value)
{
    @Html.TextBox(name, //Name of the variable
            string.IsNullOrEmpty(value) ? string.Empty : value, //Value of the variable
            new { @class = "form-control", @type="email" })
    @Html.ValidationMessage(name)
}

@helper RenderUrlBoxFormControl(System.Web.Mvc.HtmlHelper Html, string name, string value)
{
    @Html.TextBox(name, //Name of the variable
            string.IsNullOrEmpty(value) ? string.Empty : value, //Value of the variable
            new { @class = "form-control", @type = "url" })
    @Html.ValidationMessage(name)
}

@helper RenderPasswordBoxFormControl(System.Web.Mvc.HtmlHelper Html, string name, string value)
{
    @Html.TextBox(name, //Name of the variable
            string.IsNullOrEmpty(value) ? string.Empty : "*********************************************************", //Value of the variable
            new { @class = "form-control", @type = "password"})
    @Html.ValidationMessage(name)
}

@helper RenderDropDownFormControl(System.Web.Mvc.HtmlHelper Html, string name, IEnumerable<System.Web.Mvc.SelectListItem> value)
{
    @Html.DropDownList(
        name,
        value,
        new { @class = "form-control" }
    );
    @Html.ValidationMessage(name);
}

@helper RenderTextAreaFormControl(System.Web.Mvc.HtmlHelper Html, string name, string value)
{
    @Html.TextArea(name, //Name of the variable
        string.IsNullOrEmpty(value) ? string.Empty : value, //Value of the variable
        new { @class = "form-control" })
    @Html.ValidationMessage(name)
}
