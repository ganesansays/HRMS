@using HRMS.Models;
@using HRMS.BusinessEntities;

@model ModelContainer<Group>

@{
    @Html.Hidden("Id", @Model.Instance.Id)
    @Html.LabelFor(model => model.Instance.Name)
    @ControlHelper.RenderTextBoxFormControl(Html, "Name", Model.Instance.Name)
}
